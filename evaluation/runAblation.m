function results = runAblation(net)
%RUNABLATION  Ablation study: quality gate, calibration, evidence, backbone.
%
%   TODO(Sprint 8): quantify contribution of each stage to the final metrics
%   (docs/ARCHITECTURE.md §2 Stage 10). Reports honest deltas only.
%
%   Sprint 0: refuses to fabricate ablation numbers.

    raiseError('runAblation', 'NotImplemented', ...
        'Ablation runs at Sprint 8 with a real trained model.');
end